interface itCourse {
    courseName : string,
    location : string,
    onSiteStudents : number,
}

let itCourse = {
    courseName : "AI Metaverse web 0.3",
    location : "Governer House Sindh",
    onSiteStudents : 50000,
};

console.log(itCourse);
